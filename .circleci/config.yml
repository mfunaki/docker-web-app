version: 2.1

orbs:
  node: circleci/node@3.0.0

jobs:
  build-and-test:
    docker:
      - image: node:12
    steps:
      - checkout
      - run: npm install
      - run: npm test

workflows:
    version: 2.1
    build-deploy:
      jobs:
        - build-and-test
